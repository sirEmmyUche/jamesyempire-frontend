import {ToastContainer} from'react-toastify'

const UseToastContainer =()=>{
    return(
        <ToastContainer
        draggable={true}
        limit={1}
        />
    )
}

export default UseToastContainer